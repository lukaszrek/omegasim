package org.anomek.omegasim.scenarios.partysynergy;

import org.anomek.omegasim.entities.Bounds;
import org.anomek.omegasim.entities.Bounds.Bounded;

public interface WithIndex extends Bounded {
    int index();
}
